// Shared by the dev middleware and the browser-side reader, the way
// LOG_ENDPOINT is. Deliberately outside /api/fs, which stays board-scoped.
export const PROJECT_FILE_ENDPOINT = '/api/project-file';
