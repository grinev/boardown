// Injected by vite.config.ts's `define` from BOARDOWN_LOG_LEVEL.
declare const __BOARDOWN_LOG_LEVEL__: string;

// Injected by vite.config.ts's `define` from the root package.json version.
declare const __BOARDOWN_VERSION__: string;
