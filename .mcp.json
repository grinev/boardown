{
  "mcpServers": {
    "playwright": {
      "timeout": 120000,
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--browser",
        "chromium",
        "--isolated",
        "--output-dir",
        ".playwright-mcp"
      ]
    }
  }
}
